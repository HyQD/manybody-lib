[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "manybody_lib"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "opt_einsum",
    "scipy",
]

[tool.setuptools.packages.find]
include = ["manybody_lib*"]
